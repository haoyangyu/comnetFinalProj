common_change.cpp: the example 3 provided common codes
common_change.h: the example 3 provided common codes 

newport.h: the class that inhereted from the Sendport class, which inherited from Port class

sender.cpp: the implementation of sending function

receiver.cpp: the implementation of receiving function

Makefile: the build up bundle files



